# prediction-model.md — 예측 모델과 백테스트

> 예측·백테스트 전에 읽는다. 기획서의 AI 활용 근거이므로 정확도뿐 아니라
> 데이터 누수 방지, 재현 명령, 결과 보존이 필수다.

## 예측 목표와 입력

- 목표: 지역의 일별 **평년 대비 저수율 `avgRatio`**를 앞으로 30일 예측한다.
- 입력: 논가뭄지도의 한 시군 `avgRatio` 시계열. 대표 저수지 원저수율 `rate`를 넣지 않는다.
- 부가 출력: 현재 하락 추세가 유지될 때 다음 공식 단계에 이를 가능성이 있는 일수(최대 30일).
- 공식 단계·전망을 대체하지 않는다. 자체 결과는 항상 참고 정보로 표시한다.

## 후보 모델

모두 `apps/web/src/lib/prediction/models.ts`의 결정적인 TypeScript 순수 함수로 구현한다.

| 모델     | 정의                   | 역할      |
| -------- | ---------------------- | --------- |
| `naive`  | 마지막 값 유지         | 기준선 1  |
| `ma7`    | 최근 7일 평균 유지     | 기준선 2  |
| `linear` | 최근 N일 선형회귀 외삽 | 주력 후보 |
| `ses`    | 단순 지수평활          | 주력 후보 |

- 입력은 `number[]`(날짜 오름차순의 `avgRatio`), 출력은 30일 예측 `number[]`다.
  입력이 14일 미만이면 명시적 에러를 낸다(모델별 최소 길이 상수, 전 모델 14일 통일).
- 복잡한 딥러닝은 v1 범위 밖이다. 기준선과 비교한 수치로 모델 선택을 방어한다.
- N·평활계수는 이름 있는 상수와 모델 버전에 포함한다. 변경하면 리포트를 다시 만든다.
- 확정 상수(단계 3 플랜 승인): `LINEAR_WINDOW_DAYS = 14`, `MA_WINDOW_DAYS = 7`,
  `SES_ALPHA = 0.3`, `MODEL_VERSION = "pred-v1"`.
- “더 단순한 모델” 동률 판정 서열(`MODEL_SIMPLICITY_ORDER`, 앞이 더 단순):
  `naive < ma7 < ses < linear`.

## 백테스트 프로토콜

재현 명령은 스캐폴드 후 `pnpm backtest`로 고정한다.

1. 논가뭄지도에서 유효 관측 180일 이상인 모든 시군 시계열을 사용한다.
2. 각 시계열 마지막 90일 안에서 14일 간격의 rolling origin을 만들며 미래값을 학습에 넣지 않는다.
   가장 최근 origin은 `lastDate − 지평일수`라 모든 origin이 꽉 찬 평가 창을 갖는다.
3. 각 origin에서 7일·14일을 예측해 모델별 MAE(%p)와 RMSE(%p)를 계산한다.
4. 지역별 지표와 지역별 MAE의 단순 평균(macro average)을 모두 낸다.
5. 기본 모델은 **14일 macro MAE 최저**로 선택한다. 차이가 0.05%p 이하면 더 단순한 모델을 고른다.
6. 관측 수 부족·긴 결측 구간은 제외하고 지역 코드·제외 이유를 리포트에 남긴다.
7. `data/backtest-report.json`에 원천 체크섬, 실행 시각, git commit, 모델 파라미터,
   표본·origin 수, 전체 지표를 기록한다.

구현 확정(단계 3 플랜 승인): origin은 `lastDate − 14·k (k=1..6)`로 만들어 마지막
90일 안에서 14일 간격을 이루고, 각 origin 뒤 30일 평가 구간이 시계열 끝을 넘지 않는다.
7일 지표는 horizon 1~~7, 14일 지표는 horizon 1~~14 잔차(실측−예측)의 MAE/RMSE다.
제외 규칙: 유효 관측 180일 미만은 `insufficient_days`, 연속 결측 7일 초과
(`MAX_GAP_DAYS = 7`)는 `long_gap`. 모든 지표는 소수 4자리로 반올림해 기록한다
(재실행 재현성 비교의 기준). 엔진은 `apps/web/src/lib/prediction/backtest.ts`의
순수 함수이고 실행 시각·git commit은 CLI(`apps/web/scripts/backtest.ts`)가 주입한다.
리포트 형태는 `apps/web/src/lib/prediction/backtest-report.ts`의 Zod 스키마가 단일 출처다.

서비스 출시 전 이 문서의 “결과” 절에 리포트 요약과 채택 근거를 작성한다.

## 다음 단계 도달 가능 시점

현재 공식 단계의 바로 아래 임계값만 계산한다.

```
r0 = 최신 avgRatio
d  = 선택 모델이 암시하는 일일 변화량(%p/day)
t  = 다음 단계 임계값(70, 60, 50, 40)

d < 0 이고 r0 > t 이면 days = ceil((r0 - t) / abs(d))
그 외에는 30일 안 도달 가능성 없음
```

- `d`의 정의(2026-07-22 확정): **최근 14일 관측값의 선형 회귀 기울기**
  `observedDailyDelta(series)` (%p/day)다. 백테스트 채택 모델(naive 등 수평
  예측 모델)은 추세를 표현할 수 없으므로, 추세·도달일은 관측 기울기에서,
  30일 예측선·불확실성 밴드는 채택 모델과 잔차 분위수에서 각각 도출한다.
  두 지표의 근거가 다름을 API 메타데이터와 화면 문구("최근 흐름이 이어지면")로
  구분해 표시한다. (이전 정의 `(forecast[13] − r0) / 14`는 폐기 —
  naive 채택 시 항상 0이 되어 도달일 기능이 무력화된다.)
- 추세 버킷: `|d| < 0.05`(`TREND_STABLE_EPSILON`)이면 `stable`, 그 외 음수는
  `falling`, 양수는 `rising`. 경계값 ±0.05는 방향을 유지한다.
- 도달 버킷: `days` 없음 → `none`, ≤7 → `within_7d`, ≤14 → `within_14d`,
  ≤30 → `within_30d`.
- 정상→관심 70, 관심→주의 60, 주의→경계 50, 경계→심각 40을 사용한다.
  임계값은 `apps/web/src/lib/data/drought-stage.ts`만 import한다(복제 금지).
- 이미 심각이면 다음 단계 도달일을 계산하지 않는다.
- `days`가 1~30일일 때만 숫자를 표시한다. 그 밖에는 “안정”과 보조 문구
  “현재 추세로는 30일 안에 다음 단계 가능성이 낮아요”를 표시한다.
- 예: `r0=68`, `d=-0.45`, `t=60`이면 `ceil(8/0.45)=18일`이다.
- 예: `r0=46`, `d=-0.67`, `t=40`이면 `ceil(6/0.67)=9일`이다.

## 불확실성·표현

- 실측(최근 60일)=실선, 30일 예측=점선, 불확실 구간=밴드로 구분한다.
- 백테스트 잔차가 충분하면 horizon별 경험적 25~75 분위수(50% 구간)를 밴드로 쓴다.
  부족하면 최근 14일 MAE를 “예측 오차 ±X.X%p”로 표시하고 밴드 산식을 API 메타데이터에 남긴다.
- 지역별 `bandScale`(지역 rmse14 / 전체 macro rmse14, 0.6–1.6 클램프)를 분위수에 곱해
  안정 지역의 밴드는 좁히고 변동 큰 지역은 넓힌다.
- 숫자 문구: **“지금 추세가 이어지면 N일 뒤 ‘단계’에 들어설 가능성이 있어요.”**
- 확정 표현(“내려가요”, “됩니다”, “위험합니다”)은 사용하지 않는다.
- 모든 예측 카드·상세에 “예측은 참고용이며 공식 가뭄 예·경보가 우선이에요.”를 표시한다.
- 공식 1·2·3개월 전망에는 “한국농어촌공사 공식 전망”과 발행일을 붙여 자체 예측과 구별한다.

## 만수위 참고 로직

대표 저수지의 원저수율 `rate >= 95`이고 최근 원저수율 추세가 상승할 때만 파란 “참고”
배너와 배수로 점검 행동을 표시한다. 지역 `avgRatio`로 이 조건을 판단하지 않는다.
독자적인 홍수 단계나 경보를 만들지 않는다.

## LLM 행동 코치

정량 예측과 자연어 설명은 분리한다. 이 문서의 순수 함수가 `avgRatio`, 공인 단계,
도달 가능 시점과 오차를 계산하고, LLM은 그 값을 계산·수정하지 않는다. 서버가 선택한
행동 ID·개수·순서를 보존한 쉬운 이유만 생성한다. 상세 호출·캐시·비용·평가 규칙은
`docs/llm-coach.md`를 따른다.

## 결과 — 2026-07-26 재실행 (`pnpm backtest`, 아래 수치는 전부 리포트 실측값)

> 예측 지평을 **14일 → 30일**로 늘리면서(차트를 앞뒤로 최소 한 달씩 보여 달라는 현장 요구)
> 백테스트를 다시 돌렸다. 가장 최근 origin이 `lastDate − 30`으로 밀려 모든 origin이 꽉 찬
> 30일 평가 창을 갖고, origin 창(90일) 안이라는 제약 때문에 지역당 origin이 6 → 5개가 됐다.
> `mae14`는 이제도 **horizon 1~14만** 걸러 계산한다(30일치 평균이 아니다).

- [x] 원천 체크섬 기록 —
      `data/raw/한국농어촌공사_논가뭄지도_20251231.csv`(2025-01-01~12-31) SHA-256
      `02d2392898289ab5c473b5c1c6c9220f8ade84bbeed4f4e68507a8da2efad319`.
      `normalizeDroughtMap` 정규화 결과 적재 56,258행, 격리 4,697행
      (placeholder·stage_mismatch 등 — `build:data`와 동일 파이프라인).
- [x] 지역·origin 수와 제외 내역 기록 — 평가 154개 시군 × origin 5개 = 770 origin,
      잔차 표본 23,100개(770 × 30 horizon). 제외 1곳: `41450`
      (`insufficient_days` — 유효 관측 180일 미만). `long_gap` 제외 없음.
- [x] 모델별 7·14·30일 MAE/RMSE 표 기록 (%p, 지역별 지표의 macro average):

  | 모델             | 7일 MAE    | 7일 RMSE   | 14일 MAE   | 14일 RMSE  | 30일 MAE   | 30일 RMSE  |
  | ---------------- | ---------- | ---------- | ---------- | ---------- | ---------- | ---------- |
  | **naive (채택)** | **1.8493** | **3.3725** | **3.0463** | **5.2929** | **4.3201** | **6.7554** |
  | damped           | 2.2156     | 3.6687     | 3.4536     | 5.5316     | 4.7857     | 7.1216     |
  | ses              | 2.3209     | 3.7402     | 3.4152     | 5.5054     | 4.5889     | 6.8747     |
  | ma7              | 2.4938     | 3.9043     | 3.5603     | 5.6121     | 4.7056     | 6.9513     |
  | linear           | 3.3464     | 5.1344     | 5.2300     | 7.9536     | 8.7553     | 13.6351    |

- [x] 채택 모델·파라미터·근거 기록 — **naive**(`pred-v1`). 근거: 14일 macro MAE
      최저(3.0463)를 단독 달성, 2위 ses(3.4152)와 차이 0.3689%p > 0.05%p로 동률 규칙
      미적용(`rule: "lowest_mae14"`, `tiedWith: []`). 30일 MAE에서도 naive(4.3201)가
      최저라 지평을 늘려도 선택이 뒤집히지 않았다. 1년 데이터에서는 기준선이 주력
      후보(linear·ses)보다 오차가 낮았다 — 기준선과 비교한 수치로 선택을 방어한다는
      이 문서의 원칙 그대로 채택한다. 파라미터: `LINEAR_WINDOW_DAYS=14`,
      `MA_WINDOW_DAYS=7`, `SES_ALPHA=0.3`, `MIN_VALID_DAYS=180`, `MAX_GAP_DAYS=7`,
      `FORECAST_HORIZON_DAYS=30`, origin 창 90일·간격 14일, 지표 소수 4자리.
- [x] `data/backtest-report.json` 커밋 — 채택 모델의 horizon 1~30 잔차 경험적
      p25/p75(50% 구간) 포함(h7 −1.2~+0.8, h14 −2.1~+1.4, h30 −3.9~+1.4,
      horizon당 770표본). 지평이 길수록 밴드가 넓어지는 것이 그대로 드러난다.
      지역별 `bandScale`(지역 rmse14 / macro rmse14 5.2929, 0.6–1.6 클램프)도 함께 기록한다
      (예: 44230 논산 1.6, 46170 나주 0.79). 밴드 산식 메타데이터는
      `residual_quantile_p25_p75_regional`.

결과가 채워지기 전에는 발표자료나 UI에 임의 정확도 수치를 쓰지 않는다.


## 모델 재검증 (2026-07-26)

추세가 이어지는 지역에서 수평 예측이 과소평가하는지 확인하려고 **damped(감쇠 추세)** 모델을
후보에 추가해 같은 백테스트로 비교했다. φ=0.8로 최근 14일 OLS 기울기를 하루마다 감쇠시켜 더한다.

| 모델 | MAE 7일 | MAE 14일 | MAE 30일 |
|---|---|---|---|
| naive | **1.8493** | **3.0463** | **4.3201** |
| damped | 2.2156 | 3.4536 | 4.7857 |
| ses | 2.3209 | 3.4152 | 4.5889 |
| ma7 | 2.4938 | 3.5603 | 4.7056 |
| linear | 3.3464 | 5.2300 | 8.7553 |

154개 지역·770 origin·23,100 표본 기준으로 **naive가 여전히 최적**이라 채택 모델을 바꾸지 않았다.
damped는 후보로 남겨 다음 재검증 때 다시 비교한다(추세 반영 요구는 예측선이 아니라 “이 추세라면”
문구가 관측 기울기로 처리한다).
